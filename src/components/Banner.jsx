
const Banner = ({className}) => {
     
    
    if (className) {
        return(
            <div className={`bannerBoiler  ${className}`}>
        
            </div>
        );
        }
        
}

export default Banner;