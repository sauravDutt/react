
const Banner = ({className}) => {
     
    
    if (className) {
        return(
            <div className={`${className}`}>
        
            </div>
        );
        }
        
}

export default Banner;